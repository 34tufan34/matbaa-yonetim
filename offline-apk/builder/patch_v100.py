from pathlib import Path
p=Path('app/src/main/assets/index.html')
html=p.read_text(encoding='utf-8')

html=html.replace('v0.9.1 • Android / tamamen offline','v1.0.0 • Android / tamamen offline')
html=html.replace("appVersion:'0.9.0'", "appVersion:'1.0.0'")

needle='''          <div class="report-filter-v2">
            <label><span>Başlangıç</span><input type="date" id="reportStart" /></label>
            <label><span>Bitiş</span><input type="date" id="reportEnd" /></label>
            <label><span>Vardiya</span><select id="reportShiftFilter"></select></label>
            <label><span>Makine</span><select id="reportMachineFilter"></select></label>
            <label><span>Usta</span><select id="reportMasterFilter"></select></label>
            <button id="runReportBtn" class="btn primary report-run-btn">Raporu Güncelle</button>
          </div>
        </div>'''
replace='''          <div class="report-filter-v2">
            <label><span>Başlangıç</span><input type="date" id="reportStart" /></label>
            <label><span>Bitiş</span><input type="date" id="reportEnd" /></label>
            <label><span>Vardiya</span><select id="reportShiftFilter"></select></label>
            <label><span>Makine</span><select id="reportMachineFilter"></select></label>
            <label><span>Usta</span><select id="reportMasterFilter"></select></label>
            <button id="runReportBtn" class="btn primary report-run-btn">Raporu Güncelle</button>
          </div>
          <div class="report-detail-tabs" id="reportDetailTabs">
            <button class="active" data-report-detail="general">Genel</button>
            <button data-report-detail="shift">Vardiya</button>
            <button data-report-detail="machine">Makine</button>
            <button data-report-detail="master">Usta</button>
            <button data-report-detail="scrap">Fire</button>
            <button data-report-detail="downtime">Duruş</button>
            <button data-report-detail="workorder">İş Emri</button>
            <button data-report-detail="mix">Renk & Lak</button>
            <button data-report-detail="trend">Trend</button>
            <button data-report-detail="compare">Karşılaştırma</button>
          </div>
        </div>'''
if needle not in html: raise SystemExit('filter needle missing')
html=html.replace(needle,replace)

needle='''        <div class="report-detail-grid">
          <div class="panel">
            <div class="panel-head"><div><div class="eyebrow">İŞ KARMASI</div><h3>Renk / İş Dağılımı</h3></div></div>
            <div id="colorReport" class="compact-report"></div>
          </div>
          <div class="panel">
            <div class="panel-head"><div><div class="eyebrow">ÖZELLİK</div><h3>Lak / UV Dağılımı</h3></div></div>
            <div id="coatingReport" class="compact-report"></div>
          </div>
        </div>
      </section>'''
replace='''        <div class="report-detail-grid">
          <div class="panel">
            <div class="panel-head"><div><div class="eyebrow">İŞ KARMASI</div><h3>Renk / İş Dağılımı</h3></div></div>
            <div id="colorReport" class="compact-report"></div>
          </div>
          <div class="panel">
            <div class="panel-head"><div><div class="eyebrow">ÖZELLİK</div><h3>Lak / UV Dağılımı</h3></div></div>
            <div id="coatingReport" class="compact-report"></div>
          </div>
        </div>
        <div id="reportDetailHost" class="extended-report-host"></div>
      </section>'''
if needle not in html: raise SystemExit('report tail needle missing')
html=html.replace(needle,replace)

css=r'''
.report-detail-tabs{display:flex;gap:7px;align-items:center;overflow-x:auto;padding:12px 0 2px;margin-top:4px;scrollbar-width:thin}.report-detail-tabs button{flex:0 0 auto;border:1px solid #29313e;background:#0d1117;color:#9fa9b7;border-radius:999px;padding:8px 12px;font-size:10px;font-weight:800;cursor:pointer}.report-detail-tabs button.active{background:#1d2632;color:#fff;border-color:#4e5e74}.extended-report-host{display:grid;gap:12px;margin-top:14px}.xreport-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.xreport-card{background:linear-gradient(180deg,#121821,#0e131a);border:1px solid #252e3a;border-radius:12px;padding:13px;min-height:84px}.xreport-card span{display:block;color:#7d8897;font-size:9px;text-transform:uppercase;letter-spacing:.55px;font-weight:800}.xreport-card strong{display:block;margin-top:7px;font-size:19px}.xreport-card small{display:block;margin-top:4px;color:#778292;font-size:9px}.xreport-card.bad{border-color:#5c2931}.xreport-card.good{border-color:#285340}.xreport-two{display:grid;grid-template-columns:1fr 1fr;gap:12px}.xreport-panel{background:linear-gradient(180deg,#12161d,#101319);border:1px solid var(--line);border-radius:14px;padding:15px;overflow:hidden}.xreport-panel h3{margin:0;font-size:14px}.xreport-panel p{margin:5px 0 12px;color:#7f8997;font-size:10px}.xreport-table-wrap{overflow:auto;border:1px solid #232b36;border-radius:10px}.xreport-table{width:100%;border-collapse:collapse;min-width:880px;font-size:10px}.xreport-table th{position:sticky;top:0;background:#151b24;color:#9aa5b4;text-align:right;padding:9px 8px;border-bottom:1px solid #2a3340;white-space:nowrap;z-index:1}.xreport-table th:first-child,.xreport-table td:first-child{text-align:left;position:sticky;left:0;background:#11161d;z-index:2}.xreport-table td{padding:9px 8px;text-align:right;border-bottom:1px solid #202731;white-space:nowrap;color:#d8dee7}.xreport-table tr:last-child td{border-bottom:0}.xreport-table tbody tr{cursor:pointer}.xreport-table tbody tr:hover td{background:#161d27}.xreport-table tbody tr:hover td:first-child{background:#1a222d}.xreport-name strong{display:block;font-size:11px}.xreport-name span{display:block;color:#788392;font-size:9px;margin-top:2px}.xreport-rank{display:inline-grid;place-items:center;width:20px;height:20px;border:1px solid #303a48;border-radius:6px;margin-right:6px;color:#8f9bac;font-size:9px}.xreport-badge{display:inline-flex;padding:4px 7px;border-radius:999px;border:1px solid #303946;background:#111820;font-size:9px}.xreport-badge.good{color:#7ee0ae;border-color:#285941;background:#14261d}.xreport-badge.bad{color:#ff9eaa;border-color:#61303a;background:#2d171c}.xreport-findings{display:grid;gap:7px}.xreport-finding{display:flex;gap:9px;align-items:flex-start;padding:9px 10px;background:#0d1218;border:1px solid #242d38;border-radius:9px;font-size:10px;line-height:1.45}.xreport-finding i{width:7px;height:7px;border-radius:50%;background:#65758b;margin-top:4px;flex:0 0 auto}.xreport-finding.good i{background:#55c891}.xreport-finding.bad i{background:#e46672}.xreport-finding.warn i{background:#d0aa50}.xreport-bars{display:grid;gap:8px}.xreport-bar{display:grid;grid-template-columns:minmax(120px,1fr) 120px 70px;gap:8px;align-items:center;font-size:10px}.xreport-bar .track{height:7px;border-radius:99px;background:#202833;overflow:hidden}.xreport-bar .track i{display:block;height:100%;background:#8c9aad;border-radius:99px}.xreport-bar strong{text-align:right}.xreport-note{padding:10px 12px;border-radius:9px;border:1px solid #453a1d;background:#211c11;color:#d7c27d;font-size:10px;line-height:1.5}.xreport-empty{padding:24px;text-align:center;color:#778291;font-size:11px}.xreport-section-title{display:flex;justify-content:space-between;align-items:end;gap:12px}.xreport-section-title h2{margin:0;font-size:17px}.xreport-section-title p{margin:4px 0 0;color:#7f8997;font-size:10px}.xreport-section-title .subtle-chip{flex:0 0 auto}.coverage-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px}.coverage-item{border:1px solid #252d38;border-radius:10px;background:#0d1218;padding:10px}.coverage-item span{display:block;font-size:9px;color:#7f8997;text-transform:uppercase}.coverage-item strong{display:block;margin-top:5px;font-size:15px}.coverage-item .mini-track{height:5px;background:#202833;border-radius:9px;overflow:hidden;margin-top:7px}.coverage-item .mini-track i{display:block;height:100%;background:#8695aa}.trend-split{display:grid;grid-template-columns:1.2fr .8fr;gap:12px}.xreport-mini-table{display:grid;gap:4px}.xreport-mini-row{display:grid;grid-template-columns:minmax(90px,1fr) repeat(4,minmax(70px,.7fr));gap:8px;padding:8px 9px;border:1px solid #232b36;border-radius:8px;background:#0d1218;font-size:10px;align-items:center}.xreport-mini-row.head{background:#151b23;color:#8793a3;text-transform:uppercase;font-size:8px;font-weight:800}.xreport-mini-row span:not(:first-child),.xreport-mini-row strong{text-align:right}.xreport-mini-row.clickable{cursor:pointer}.xreport-mini-row.clickable:hover{border-color:#3a4657;background:#111821}@media(max-width:1100px){.xreport-grid{grid-template-columns:repeat(2,1fr)}.xreport-two,.trend-split{grid-template-columns:1fr}.coverage-grid{grid-template-columns:repeat(3,1fr)}}@media(max-width:650px){.xreport-grid{grid-template-columns:1fr 1fr}.coverage-grid{grid-template-columns:1fr 1fr}.report-detail-tabs{margin-left:-4px;margin-right:-4px}.xreport-card strong{font-size:16px}}
'''
html=html.replace('</style>',css+'\n</style>',1)

marker='function drillScopeName(scope)'
idx=html.index(marker)
js=r'''
let currentReportDetail='general';
function recordMinutes(r){let m=Number(r.jobDurationMin||0);if(m>0)return m;const cv=t=>{const p=String(t||'').split(':').map(Number);return p.length===2&&p.every(Number.isFinite)?p[0]*60+p[1]:0};if(r.startTime&&r.endTime){let a=cv(r.startTime),b=cv(r.endTime);if(b<a)b+=1440;return Math.max(0,b-a)}return 0}
function knownReasonStats(rows,key,source,valueKey,unknownName='Belirtilmemiş'){const map=new Map();rows.forEach(r=>{const value=Number(r[valueKey]||0);if(!value)return;const id=r[key]||'unknown',name=id==='unknown'?unknownName:(getById(source,id)?.name||'Tanımsız'),cur=map.get(id)||{id,name,value:0};cur.value+=value;map.set(id,cur)});return [...map.values()].sort((a,b)=>b.value-a.value)}
function enhancedGroupStats(rows,key,source){const m=new Map();rows.forEach(r=>{const id=r[key]||'none';if(!m.has(id))m.set(id,[]);m.get(id).push(r)});return [...m.entries()].map(([id,rr])=>{const good=sum(rr,'goodQty'),scrap=sum(rr,'scrapQty'),gross=good+scrap,down=sum(rr,'downtimeMin'),plates=sum(rr,'plateQty'),jobs=uniqueJobs(rr),duration=rr.reduce((a,r)=>a+recordMinutes(r),0),name=id==='none'?(key==='masterId'?'Usta bilgisi yok':'Tanımsız'):(getById(source,id)?.name||'Tanımsız'),machine=key==='machineId'?getById(state.machines,id):null,capacity=Number(machine?.hourlyCapacity||0),theoretical=capacity&&duration?capacity*duration/60:0;return{id,name,rows:rr,good,scrap,gross,rate:gross?scrap*100/gross:0,down,plates,jobs,records:rr.length,duration,hourly:duration?good*60/duration:0,avgJob:jobs?good/jobs:0,downPerJob:jobs?down/jobs:0,capacityUse:theoretical?good*100/theoretical:null}}).sort((a,b)=>b.good-a.good)}
function workOrderStats(rows){const m=new Map();rows.forEach(r=>{const k=String(r.workOrder||'').trim()||'(İş emri yok)';if(!m.has(k))m.set(k,[]);m.get(k).push(r)});return [...m.entries()].map(([name,rr])=>{const s=statsOf(rr),duration=rr.reduce((a,r)=>a+recordMinutes(r),0);return{id:name,name,rows:rr,good:s.good,scrap:s.scrap,gross:s.good+s.scrap,rate:s.rate,down:s.down,plates:s.plates,records:rr.length,duration,hourly:duration?s.good*60/duration:0,machines:[...new Set(rr.map(r=>getById(state.machines,r.machineId)?.name).filter(Boolean))],shifts:[...new Set(rr.map(r=>getById(state.shifts,r.shiftId)?.name).filter(Boolean))],masters:[...new Set(rr.map(r=>getById(state.people,r.masterId)?.name).filter(Boolean))],colors:[...new Set(rr.map(r=>r.colors).filter(x=>x!=null&&Number(x)>0))],coating:rr.some(r=>r.uvLak===true)?'UV Lak':rr.some(r=>r.lak===true)?'Lak':rr.every(r=>r.lak===false&&r.uvLak===false)?'Yok':'Belirtilmemiş'}}).sort((a,b)=>b.good-a.good)}
function coveragePct(rows,fn){if(!rows.length)return 0;return rows.filter(fn).length*100/rows.length}
function groupByPeriod(rows,mode){const map=new Map();rows.forEach(r=>{if(!r.date)return;let key=r.date,label=r.date;if(mode==='week'){const d=new Date(r.date+'T12:00:00'),day=(d.getDay()+6)%7;d.setDate(d.getDate()-day);key=localISO(d);const e=new Date(d);e.setDate(e.getDate()+6);label=`${d.toLocaleDateString('tr-TR',{day:'2-digit',month:'2-digit'})}–${e.toLocaleDateString('tr-TR',{day:'2-digit',month:'2-digit'})}`}else if(mode==='month'){key=r.date.slice(0,7);label=new Date(key+'-01T12:00:00').toLocaleDateString('tr-TR',{month:'long',year:'numeric'})}if(!map.has(key))map.set(key,{key,label,rows:[]});map.get(key).rows.push(r)});return [...map.values()].sort((a,b)=>a.key.localeCompare(b.key)).map(x=>{const s=statsOf(x.rows);return{...x,...s,avgJob:s.jobs?s.good/s.jobs:0}})}
function groupReportTable(stats,scope){if(!stats.length)return'<div class="xreport-empty">Bu kırılım için veri yok.</div>';const avg=statsOf(currentReportRows).rate;return`<div class="xreport-table-wrap"><table class="xreport-table"><thead><tr><th>Grup</th><th>Net</th><th>Brüt</th><th>İş</th><th>Kalıp</th><th>Fire</th><th>Fire %</th><th>Duruş</th><th>Duruş/İş</th><th>Net/İş</th><th>Tabaka/Saat</th>${scope==='machine'?'<th>Kapasite %</th>':''}</tr></thead><tbody>${stats.map((x,i)=>`<tr data-xscope="${scope}" data-xvalue="${esc(x.id)}" data-xlabel="${esc(x.name)}"><td><div class="xreport-name"><strong><span class="xreport-rank">${i+1}</span>${esc(x.name)}</strong><span>${fmtNum(x.records)} kayıt</span></div></td><td>${fmtNum(x.good)}</td><td>${fmtNum(x.gross)}</td><td>${fmtNum(x.jobs)}</td><td>${fmtNum(x.plates)}</td><td>${fmtNum(x.scrap)}</td><td><span class="xreport-badge ${x.rate<=avg?'good':'bad'}">${fmtPct(x.rate)}</span></td><td>${fmtNum(x.down)} dk</td><td>${fmtNum(Math.round(x.downPerJob))} dk</td><td>${fmtNum(Math.round(x.avgJob))}</td><td>${x.hourly?fmtNum(Math.round(x.hourly)):'-'}</td>${scope==='machine'?`<td>${x.capacityUse==null?'<span class="xreport-badge">Kapasite tanımsız</span>':fmtPct(x.capacityUse)}</td>`:''}</tr>`).join('')}</tbody></table></div>`}
function bindExtendedDrill(host,rows){qsa('[data-xscope]',host).forEach(el=>el.onclick=()=>openDrill(el.dataset.xscope,el.dataset.xvalue,el.dataset.xlabel,rows))}
function barList(stats,unit=''){if(!stats.length)return'<div class="xreport-empty">Veri yok.</div>';const max=Math.max(...stats.map(x=>x.value),1),total=stats.reduce((a,b)=>a+b.value,0)||1;return`<div class="xreport-bars">${stats.slice(0,12).map(x=>`<div class="xreport-bar"><span>${esc(x.name)}</span><div class="track"><i style="width:${Math.max(2,x.value*100/max)}%"></i></div><strong>${fmtNum(x.value)} ${unit}<small style="display:block;color:#788392">${fmtPct(x.value*100/total)}</small></strong></div>`).join('')}</div>`}
function topJobTable(rows,metric='scrap'){const data=workOrderStats(rows).sort((a,b)=>metric==='down'?b.down-a.down:metric==='rate'?b.rate-a.rate:b.scrap-a.scrap).slice(0,12);if(!data.length)return'<div class="xreport-empty">İş emri verisi yok.</div>';return`<div class="xreport-mini-table"><div class="xreport-mini-row head"><span>İş Emri</span><span>Net</span><span>Fire %</span><span>Duruş</span><span>Kayıt</span></div>${data.map(x=>`<div class="xreport-mini-row clickable" data-xscope="workOrder" data-xvalue="${esc(x.name)}" data-xlabel="${esc(x.name)}"><strong>${esc(x.name)}</strong><span>${fmtNum(x.good)}</span><span>${fmtPct(x.rate)}</span><span>${fmtNum(x.down)} dk</span><span>${fmtNum(x.records)}</span></div>`).join('')}</div>`}
function generalExtended(rows){const st=statsOf(rows),machines=enhancedGroupStats(rows,'machineId',state.machines),shifts=enhancedGroupStats(rows,'shiftId',state.shifts),masters=enhancedGroupStats(rows,'masterId',state.people).filter(x=>x.id!=='none'),topMachine=machines[0],lowFire=[...machines].filter(x=>x.gross>0).sort((a,b)=>a.rate-b.rate)[0],topMaster=masters[0],topShift=shifts[0],worstJob=workOrderStats(rows).sort((a,b)=>b.rate-a.rate)[0],down=downtimeStatsCombined(rows)[0],findings=[];if(topMachine)findings.push(`<div class="xreport-finding good"><i></i><div>Üretim lideri <strong>${esc(topMachine.name)}</strong>: ${fmtNum(topMachine.good)} net tabaka.</div></div>`);if(lowFire)findings.push(`<div class="xreport-finding good"><i></i><div>En düşük fireli makine <strong>${esc(lowFire.name)}</strong>: ${fmtPct(lowFire.rate)}.</div></div>`);if(topShift)findings.push(`<div class="xreport-finding"><i></i><div>En yüksek üretimli vardiya <strong>${esc(topShift.name)}</strong>: ${fmtNum(topShift.good)} tabaka.</div></div>`);if(topMaster)findings.push(`<div class="xreport-finding"><i></i><div>En yüksek üretimli usta <strong>${esc(topMaster.name)}</strong>: ${fmtNum(topMaster.good)} tabaka.</div></div>`);if(down)findings.push(`<div class="xreport-finding bad"><i></i><div>En büyük duruş kaybı <strong>${esc(down.name)}</strong>: ${fmtNum(down.value)} dk.</div></div>`);if(worstJob&&worstJob.gross>0)findings.push(`<div class="xreport-finding warn"><i></i><div>En yüksek fire oranlı iş <strong>${esc(worstJob.name)}</strong>: ${fmtPct(worstJob.rate)}.</div></div>`);const cov=[['Usta',coveragePct(rows,r=>!!r.masterId)],['Renk',coveragePct(rows,r=>r.colors!=null&&Number(r.colors)>0)],['Lak/UV',coveragePct(rows,r=>r.lak!=null&&r.uvLak!=null)],['Fire nedeni',coveragePct(rows,r=>Number(r.scrapQty||0)===0||!!r.scrapReasonId)],['Duruş nedeni',coveragePct(rows,r=>Number(r.downtimeMin||0)===0||!!r.downtimeReasonId||!!r.downtimeBreakdown)]];return`<div class="xreport-section-title"><div><h2>Karar Özeti</h2><p>Seçili dönemden otomatik çıkarılan yönetici bulguları.</p></div><span class="subtle-chip">${fmtNum(rows.length)} kayıt</span></div><div class="xreport-grid"><div class="xreport-card"><span>Brüt Baskı</span><strong>${fmtNum(st.good+st.scrap)}</strong><small>net + fire</small></div><div class="xreport-card"><span>Ort. Fire / İş</span><strong>${fmtNum(st.jobs?Math.round(st.scrap/st.jobs):0)}</strong><small>adet / iş</small></div><div class="xreport-card"><span>Ort. Duruş / İş</span><strong>${fmtNum(st.jobs?Math.round(st.down/st.jobs):0)} dk</strong><small>iş başına kayıp</small></div><div class="xreport-card"><span>Ort. Kalıp / İş</span><strong>${st.jobs?(st.plates/st.jobs).toLocaleString('tr-TR',{maximumFractionDigits:1}):'0'}</strong><small>kalıp / iş</small></div></div><div class="xreport-two"><div class="xreport-panel"><h3>Dönemin Dikkat Çeken Noktaları</h3><p>Yalnızca kayıtlı verilere dayanır.</p><div class="xreport-findings">${findings.join('')||'<div class="xreport-empty">Yeterli veri yok.</div>'}</div></div><div class="xreport-panel"><h3>Veri Tamlık Oranı</h3><p>Raporların güvenilirliğini etkileyen alanların doluluk oranı.</p><div class="coverage-grid">${cov.map(([n,v])=>`<div class="coverage-item"><span>${n}</span><strong>${fmtPct(v)}</strong><div class="mini-track"><i style="width:${v}%"></i></div></div>`).join('')}</div>${cov.some(x=>x[1]<50)?'<div class="xreport-note" style="margin-top:10px">Bazı eski kayıt alanları eksik. Bu durum özellikle renk, lak/UV ve neden analizlerinde sonuçları sınırlar.</div>':''}</div></div>`}
function groupExtended(rows,type){const cfg={shift:['Vardiya Karşılaştırması','shiftId',state.shifts,'shift'],machine:['Makine Performansı','machineId',state.machines,'machine'],master:['Usta Performansı','masterId',state.people,'master']}[type];let st=enhancedGroupStats(rows,cfg[1],cfg[2]);if(type==='master')st=st.filter(x=>x.id!=='none');const best=st[0],low=[...st].filter(x=>x.gross>0).sort((a,b)=>a.rate-b.rate)[0],lowDown=[...st].sort((a,b)=>a.downPerJob-b.downPerJob)[0];return`<div class="xreport-section-title"><div><h2>${cfg[0]}</h2><p>Üretim, fire, duruş ve hız birlikte değerlendirilir.</p></div></div><div class="xreport-grid"><div class="xreport-card good"><span>Üretim Lideri</span><strong>${esc(best?.name||'-')}</strong><small>${best?fmtNum(best.good)+' tbk.':'-'}</small></div><div class="xreport-card good"><span>En Düşük Fire</span><strong>${esc(low?.name||'-')}</strong><small>${low?fmtPct(low.rate):'-'}</small></div><div class="xreport-card"><span>En Az Duruş / İş</span><strong>${esc(lowDown?.name||'-')}</strong><small>${lowDown?fmtNum(Math.round(lowDown.downPerJob))+' dk':'-'}</small></div><div class="xreport-card"><span>Karşılaştırılan Grup</span><strong>${fmtNum(st.length)}</strong><small>aktif veri grubu</small></div></div><div class="xreport-panel">${groupReportTable(st,cfg[3])}</div>${type==='machine'&&st.some(x=>x.capacityUse==null)?'<div class="xreport-note">Kapasite kullanım yüzdesi için Merkez Tanımlar → Makineler bölümünde saatlik kapasite tanımlanmalı.</div>':''}`}
function scrapExtended(rows){const total=sum(rows,'scrapQty'),st=statsOf(rows),reasons=knownReasonStats(rows,'scrapReasonId',state.scrapReasons,'scrapQty','Nedeni belirtilmemiş'),ma=enhancedGroupStats(rows,'machineId',state.machines).sort((a,b)=>b.scrap-a.scrap),sh=enhancedGroupStats(rows,'shiftId',state.shifts).sort((a,b)=>b.scrap-a.scrap),me=enhancedGroupStats(rows,'masterId',state.people).filter(x=>x.id!=='none').sort((a,b)=>b.scrap-a.scrap);return`<div class="xreport-section-title"><div><h2>Fire Analizi</h2><p>Fireyi adet, oran, neden, makine, vardiya ve usta açısından ayırır.</p></div></div><div class="xreport-grid"><div class="xreport-card bad"><span>Toplam Fire</span><strong>${fmtNum(total)}</strong><small>tabaka</small></div><div class="xreport-card"><span>Fire Oranı</span><strong>${fmtPct(st.rate)}</strong><small>brüt baskıya göre</small></div><div class="xreport-card"><span>Fire / İş</span><strong>${fmtNum(st.jobs?Math.round(total/st.jobs):0)}</strong><small>ortalama</small></div><div class="xreport-card"><span>Neden Kapsamı</span><strong>${fmtPct(coveragePct(rows,r=>Number(r.scrapQty||0)===0||!!r.scrapReasonId))}</strong><small>neden bilgisi tamlığı</small></div></div><div class="xreport-two"><div class="xreport-panel"><h3>Fire Pareto</h3><p>En büyük fire kaynakları.</p>${barList(reasons,'')}</div><div class="xreport-panel"><h3>En Riskli İşler</h3><p>Fire oranı en yüksek iş emirleri.</p>${topJobTable(rows,'rate')}</div></div><div class="xreport-two"><div class="xreport-panel"><h3>Makineye Göre Fire</h3>${groupReportTable(ma,'machine')}</div><div class="xreport-panel"><h3>Vardiyaya Göre Fire</h3>${groupReportTable(sh,'shift')}</div></div><div class="xreport-panel"><h3>Ustaya Göre Fire</h3>${groupReportTable(me,'master')}</div>`}
function downtimeExtended(rows){const total=sum(rows,'downtimeMin'),reasons=downtimeStatsCombined(rows),ma=enhancedGroupStats(rows,'machineId',state.machines).sort((a,b)=>b.down-a.down),sh=enhancedGroupStats(rows,'shiftId',state.shifts).sort((a,b)=>b.down-a.down),me=enhancedGroupStats(rows,'masterId',state.people).filter(x=>x.id!=='none').sort((a,b)=>b.down-a.down),jobs=uniqueJobs(rows);return`<div class="xreport-section-title"><div><h2>Duruş Analizi</h2><p>Kayıp sürenin nerede ve neden oluştuğunu gösterir.</p></div></div><div class="xreport-grid"><div class="xreport-card bad"><span>Toplam Duruş</span><strong>${fmtNum(total)} dk</strong><small>${(total/60).toLocaleString('tr-TR',{maximumFractionDigits:1})} saat</small></div><div class="xreport-card"><span>Duruş / İş</span><strong>${fmtNum(jobs?Math.round(total/jobs):0)} dk</strong><small>ortalama</small></div><div class="xreport-card"><span>En Büyük Neden</span><strong>${esc(reasons[0]?.name||'-')}</strong><small>${reasons[0]?fmtNum(reasons[0].value)+' dk':'-'}</small></div><div class="xreport-card"><span>Neden Kapsamı</span><strong>${fmtPct(coveragePct(rows,r=>Number(r.downtimeMin||0)===0||!!r.downtimeReasonId||!!r.downtimeBreakdown))}</strong><small>neden bilgisi tamlığı</small></div></div><div class="xreport-two"><div class="xreport-panel"><h3>Duruş Pareto</h3><p>Kayıp sürenin kaynak dağılımı.</p>${barList(reasons,'dk')}</div><div class="xreport-panel"><h3>En Uzun Kayıplı İşler</h3><p>Duruş süresi en yüksek iş emirleri.</p>${topJobTable(rows,'down')}</div></div><div class="xreport-two"><div class="xreport-panel"><h3>Makineye Göre Duruş</h3>${groupReportTable(ma,'machine')}</div><div class="xreport-panel"><h3>Vardiyaya Göre Duruş</h3>${groupReportTable(sh,'shift')}</div></div><div class="xreport-panel"><h3>Ustaya Göre Duruş</h3>${groupReportTable(me,'master')}</div>`}
function workOrderExtended(rows){const wo=workOrderStats(rows);return`<div class="xreport-section-title"><div><h2>İş Emri Analizi</h2><p>Aynı iş emrinin tüm kayıtlarını tek satırda birleştirir.</p></div><span class="subtle-chip">${fmtNum(wo.length)} iş</span></div><div class="xreport-panel"><div class="xreport-table-wrap"><table class="xreport-table"><thead><tr><th>İş Emri</th><th>Makine</th><th>Vardiya</th><th>Usta</th><th>Net</th><th>Fire</th><th>Fire %</th><th>Duruş</th><th>Kalıp</th><th>Süre</th><th>Tbk/Saat</th><th>Renk</th><th>Lak/UV</th></tr></thead><tbody>${wo.map(x=>`<tr data-xscope="workOrder" data-xvalue="${esc(x.name)}" data-xlabel="${esc(x.name)}"><td><div class="xreport-name"><strong>${esc(x.name)}</strong><span>${x.records} kayıt</span></div></td><td>${esc(x.machines.join(', ')||'-')}</td><td>${esc(x.shifts.join(', ')||'-')}</td><td>${esc(x.masters.join(', ')||'-')}</td><td>${fmtNum(x.good)}</td><td>${fmtNum(x.scrap)}</td><td><span class="xreport-badge ${x.rate<=statsOf(rows).rate?'good':'bad'}">${fmtPct(x.rate)}</span></td><td>${fmtNum(x.down)} dk</td><td>${fmtNum(x.plates)}</td><td>${fmtNum(x.duration)} dk</td><td>${x.hourly?fmtNum(Math.round(x.hourly)):'-'}</td><td>${esc(x.colors.join(', ')||'-')}</td><td>${esc(x.coating)}</td></tr>`).join('')}</tbody></table></div></div>`}
function mixExtended(rows){const colors=new Map();rows.forEach(r=>{const k=r.colors!=null&&Number(r.colors)>0?String(Number(r.colors)):'unknown';if(!colors.has(k))colors.set(k,[]);colors.get(k).push(r)});const colorStats=[...colors.entries()].map(([id,rr])=>{const s=statsOf(rr);return{id,name:id==='unknown'?'Belirtilmemiş':id+' renk',rows:rr,...s}}).sort((a,b)=>a.id==='unknown'?1:b.id==='unknown'?-1:Number(a.id)-Number(b.id)),coat=[['uv','UV Laklı',rows.filter(r=>r.uvLak===true)],['lak','Laklı',rows.filter(r=>r.lak===true)],['none','Lak / UV Yok',rows.filter(r=>r.lak===false&&r.uvLak===false)],['unknown','Belirtilmemiş',rows.filter(r=>r.lak==null||r.uvLak==null)]].map(([id,name,rr])=>{const s=statsOf(rr);return{id,name,rows:rr,...s}}),table=(data,scope)=>`<div class="xreport-table-wrap"><table class="xreport-table"><thead><tr><th>Tip</th><th>İş</th><th>Net</th><th>Fire</th><th>Fire %</th><th>Duruş</th><th>Kalıp</th></tr></thead><tbody>${data.map(x=>`<tr data-xscope="${scope}" data-xvalue="${esc(x.id)}" data-xlabel="${esc(x.name)}"><td><strong>${esc(x.name)}</strong></td><td>${fmtNum(x.jobs)}</td><td>${fmtNum(x.good)}</td><td>${fmtNum(x.scrap)}</td><td>${fmtPct(x.rate)}</td><td>${fmtNum(x.down)} dk</td><td>${fmtNum(x.plates)}</td></tr>`).join('')}</tbody></table></div>`;return`<div class="xreport-section-title"><div><h2>Renk & Lak Analizi</h2><p>İş karmasının üretim ve kayıp etkisini gösterir.</p></div></div><div class="xreport-two"><div class="xreport-panel"><h3>Renk Sayısına Göre</h3><p>1 renk, 2 renk, 4 renk ve diğer işler.</p>${table(colorStats,'color')}</div><div class="xreport-panel"><h3>Lak / UV Özelliğine Göre</h3><p>Laklı, UV laklı ve laksız işlerin karşılaştırması.</p>${table(coat,'coating')}</div></div>`}
function trendTable(data){return`<div class="xreport-mini-table"><div class="xreport-mini-row head"><span>Dönem</span><span>Net</span><span>Fire %</span><span>Duruş</span><span>İş</span></div>${data.map(x=>`<div class="xreport-mini-row"><strong>${esc(x.label)}</strong><span>${fmtNum(x.good)}</span><span>${fmtPct(x.rate)}</span><span>${fmtNum(x.down)} dk</span><span>${fmtNum(x.jobs)}</span></div>`).join('')}</div>`}
function trendExtended(rows){const day=groupByPeriod(rows,'day'),week=groupByPeriod(rows,'week'),month=groupByPeriod(rows,'month');return`<div class="xreport-section-title"><div><h2>Trend Analizi</h2><p>Üretim, fire ve duruşun zaman içindeki yönünü gör.</p></div></div><div class="trend-split"><div class="xreport-panel"><h3>Günlük Trend</h3><p>Seçili tarih aralığındaki günlük sonuçlar.</p>${trendTable(day)}</div><div class="xreport-panel"><h3>Haftalık Trend</h3><p>Haftalar arası değişim.</p>${trendTable(week)}</div></div><div class="xreport-panel"><h3>Aylık Trend</h3><p>Birden fazla ayı kapsayan seçimlerde aylık seyir.</p>${trendTable(month)}</div>`}
function comparisonExtended(rows){const start=$('reportStart').value,end=$('reportEnd').value;if(!start||!end)return'<div class="xreport-note">Karşılaştırma için başlangıç ve bitiş tarihi seçmelisin.</div>';const[ps,pe]=previousPeriodRange(start,end),prev=filterReportRows(ps,pe),cur=statsOf(rows),old=statsOf(prev),metrics=[['Net Baskı',cur.good,old.good,false,''],['Fire Oranı',cur.rate,old.rate,true,'pct'],['Duruş',cur.down,old.down,true,''],['İş Sayısı',cur.jobs,old.jobs,false,''],['Kalıp',cur.plates,old.plates,false,'']],cards=metrics.map(([n,c,p,inv,type])=>{const d=type==='pct'?c-p:pctChange(c,p),good=inv?d<0:d>0;return`<div class="xreport-card ${good?'good':d===0?'':'bad'}"><span>${n}</span><strong>${type==='pct'?fmtPct(c):fmtNum(c)}</strong><small>Önceki: ${type==='pct'?fmtPct(p):fmtNum(p)} • ${type==='pct'?deltaText(d,' puan'):deltaText(d,'%')}</small></div>`}).join(''),weeks=groupByPeriod(rows,'week');return`<div class="xreport-section-title"><div><h2>Dönem Karşılaştırması</h2><p>${esc(formatRange(start,end))} ↔ ${esc(formatRange(ps,pe))}</p></div></div><div class="xreport-grid">${cards}</div><div class="xreport-two"><div class="xreport-panel"><h3>Bu Dönemin Haftaları</h3><p>Seçili dönemi hafta hafta incele.</p>${trendTable(weeks)}</div><div class="xreport-panel"><h3>Önceki Dönem Özeti</h3><p>Eşit uzunluktaki önceki dönem.</p><div class="xreport-grid" style="grid-template-columns:1fr 1fr"><div class="xreport-card"><span>Net</span><strong>${fmtNum(old.good)}</strong></div><div class="xreport-card"><span>Fire %</span><strong>${fmtPct(old.rate)}</strong></div><div class="xreport-card"><span>Duruş</span><strong>${fmtNum(old.down)} dk</strong></div><div class="xreport-card"><span>İş</span><strong>${fmtNum(old.jobs)}</strong></div></div></div></div>`}
function renderExtendedReport(view,rows){currentReportDetail=view||currentReportDetail||'general';qsa('#reportDetailTabs button').forEach(b=>b.classList.toggle('active',b.dataset.reportDetail===currentReportDetail));const host=$('reportDetailHost');if(!host)return;if(!rows.length){host.innerHTML='<div class="xreport-panel"><div class="xreport-empty">Seçilen filtrelerde rapor verisi yok.</div></div>';return}let out='';if(currentReportDetail==='general')out=generalExtended(rows);else if(['shift','machine','master'].includes(currentReportDetail))out=groupExtended(rows,currentReportDetail);else if(currentReportDetail==='scrap')out=scrapExtended(rows);else if(currentReportDetail==='downtime')out=downtimeExtended(rows);else if(currentReportDetail==='workorder')out=workOrderExtended(rows);else if(currentReportDetail==='mix')out=mixExtended(rows);else if(currentReportDetail==='trend')out=trendExtended(rows);else if(currentReportDetail==='compare')out=comparisonExtended(rows);host.innerHTML=out;bindExtendedDrill(host,rows)}
'''
html=html[:idx]+js+'\n'+html[idx:]

old="""  $('trendRange').textContent=formatRange($('reportStart').value,$('reportEnd').value);
  drawDailyTrend($('dailyTrendChart'),rows);
}"""
new="""  $('trendRange').textContent=formatRange($('reportStart').value,$('reportEnd').value);
  drawDailyTrend($('dailyTrendChart'),rows);
  renderExtendedReport(currentReportDetail,rows);
}"""
if old not in html: raise SystemExit('runReport hook missing')
html=html.replace(old,new,1)

old="""  qsa('#periodTabs button').forEach(b=>b.onclick=()=>setPeriod(b.dataset.period)); $('runReportBtn').onclick=runReport; ['reportStart','reportEnd','reportShiftFilter','reportMachineFilter','reportMasterFilter'].forEach(id=>$(id).onchange=runReport); $('exportReportCsv').onclick=exportCsv; if($('printReportBtn')) $('printReportBtn').onclick=printMainReport; qsa('#performanceTabs button').forEach(b=>b.onclick=()=>{ currentPerformanceDimension=b.dataset.dimension; runReport(); });"""
new="""  qsa('#periodTabs button').forEach(b=>b.onclick=()=>setPeriod(b.dataset.period)); $('runReportBtn').onclick=runReport; ['reportStart','reportEnd','reportShiftFilter','reportMachineFilter','reportMasterFilter'].forEach(id=>$(id).onchange=runReport); $('exportReportCsv').onclick=exportCsv; if($('printReportBtn')) $('printReportBtn').onclick=printMainReport; qsa('#performanceTabs button').forEach(b=>b.onclick=()=>{ currentPerformanceDimension=b.dataset.dimension; runReport(); }); qsa('#reportDetailTabs button').forEach(b=>b.onclick=()=>{currentReportDetail=b.dataset.reportDetail;renderExtendedReport(currentReportDetail,currentReportRows);});"""
if old not in html: raise SystemExit('bind hook missing')
html=html.replace(old,new,1)

p.write_text(html,encoding='utf-8')
print('patched-v1',len(html))